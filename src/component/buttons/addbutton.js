import React from "react";
import "./addbtn.css"

export function AddButton({}){
    return(
        <button
        className="action-add-button btn-primary"
        onClick={() => ""}
      >
        +
      </button>
    )
}